# DocxTool WPS 侧边栏阅读功能 PRD

## 1. 文档信息

- 产品名称：DocxTool WPS 阅读模式
- 版本：5.4
- 文档状态：已实施
- 实现状态：本地 TXT Reader
- 展示名称：阅读
- 支持格式：首版仅 TXT

## 2. 产品背景

DocxTool WPS 侧边栏已具备预览排版、一键排版、清除预览和识别结果展示。新增阅读模式后，用户可以在同一侧边栏中导入和阅读本地 TXT，同时保持识别、排版功能不受影响。

阅读功能必须独立于 DocxTool Core、HostBridge 排版命令槽和 WPS 文档对象。

## 3. 产品目标

1. 支持本地 TXT 小说的导入、选择、删除和持久保存。
2. 支持自动滚动、速度控制和独立阅读进度。
3. 在“排版”和“阅读”之间快速切换，互不清空状态。
4. 提供低调显示、鼠标移出隐藏和一键返回排版界面。
5. 小说不上传网络，正文不得进入业务日志。
6. 打开阅读模式不得影响 WPS 文档输入、Ribbon 和文档切换。

## 4. 页面结构

```text
DocxTool WPS                         关闭

[ 排版 ] [ 阅读 ]

排版模式：
  原有业务按钮
  执行状态
  识别结果

阅读模式：
  [当前书籍▼] [导入] [书库] [隐身]
  ─────────────────────
  小说正文滚动区域
  ─────────────────────
  [上一段] [播放/暂停] [下一段]
  速度滑块              阅读进度
```

模式切换放在现有内容区，不增加顶部 Header 高度，不使用 `fixed` 或 `sticky` 布局。

## 5. 功能需求

| 模块 | 首版需求 |
|---|---|
| 模式切换 | 排版、阅读互斥显示；切换不清空识别结果或阅读进度 |
| TXT 导入 | 支持选择本地 TXT，复制到 DocxTool 托管目录 |
| 编码 | 支持 UTF-8、UTF-8 BOM、GB18030/GBK；无法解码时明确提示 |
| 书库 | 下拉选择、显示当前书籍、删除托管副本、删除前确认 |
| 文本解析 | 自动识别常见章节；无章节时按段落边界分块 |
| 阅读显示 | 连续滚动、上一段、下一段、上一章、下一章 |
| 自动滚动 | 播放、暂停、速度选择、到底停止 |
| 阅读进度 | 每本书独立保存章节、文本位置和滚动比例 |
| 隐身模式 | 鼠标移出隐藏正文并暂停，移入恢复显示 |
| 安全切换 | 一次操作返回真实排版界面并保存进度 |
| 样式 | 字号、行距、三种主题、透明度 |
| 快捷键 | 阅读区聚焦时，空格播放暂停、左右键切换文本 |
| 错误提示 | 编码失败、空文件、存储不足、文件丢失分别提示 |

## 6. 本地存储

权威数据目录：

```text
%LOCALAPPDATA%\DocxTool\reader\
├─ books\
│  └─ <book_id>.txt
├─ reader.db
└─ temp\
```

路径不得写死用户名或盘符，并支持 `DOCXTOOL_HOME` 覆盖。其他系统使用对应的用户数据目录。

存储职责：

- 小说正文：保存为 `books/<book_id>.txt`，不写入 SQLite。
- SQLite：保存书籍信息、章节索引、进度和阅读设置。
- 临时目录：导入成功或失败后及时清理。
- 原始 TXT：只读取，不修改、不移动、不删除。
- 删除书籍：只删除 DocxTool 管理的副本和对应数据库记录。

## 7. 数据模型

```text
books
  id, display_name, stored_filename, encoding,
  size_bytes, chapter_count, imported_at

chapters
  book_id, chapter_index, title,
  start_offset, end_offset

progress
  book_id, chapter_index, text_offset,
  scroll_ratio, updated_at

settings
  key, value
```

SQLite 使用现有标准库能力，不新增数据库依赖。小说正文不得作为 BLOB 或完整 TEXT 存入数据库。

## 8. 交互规则

1. 导入成功后自动选择新书，并从开头显示。
2. 切换书籍前保存当前书籍进度。
3. 重新打开侧边栏时恢复上次选择的书籍和位置，但默认保持暂停。
4. 手动滚动、文本选择、模式切换和页面隐藏时暂停自动滚动。
5. 启用“离开时折叠”后，隐身模式隐藏并暂停；返回后保持暂停。
6. 删除当前书籍后选择书库中的下一本；书库为空时显示导入入口。
7. 快捷键只绑定阅读区域，不能截获 WPS 文档中的字母和方向键。

## 9. 通信与解耦

```text
TaskPane 阅读界面
        ↓ 现有 Token 与 Origin 校验的 loopback HTTP
apps/wps/control/reader_routes.py
        ↓
apps/reader/ReaderService
        ├─ reader.db
        └─ books/*.txt
```

- 阅读接口不进入 HostBridge 或 CommandMonitor。
- 阅读操作不调用 `Application`、`Document`、`Range` 或 `Comments`。
- 阅读空闲状态不轮询 WPS API。
- 排版、预览和清除预览调用链保持不变。
- 本地接口继续使用现有会话 Token、Origin 策略和随机 `127.0.0.1` Control Server；不新增端口、Token、Daemon 或公网服务。

## 10. 日志与隐私

允许记录：

- 短书籍 ID、文件字节数、编码、章节数、耗时、状态和错误码。

禁止记录：

- 小说正文、书名、原文件名、绝对路径、Token、章节标题和搜索内容。

必要事件包括导入开始/完成/失败、选书、删除、播放、暂停、进度保存和存储失败。自动滚动每一帧不得写日志。

## 11. 性能要求

- TXT 解码和章节解析不得阻塞 TaskPane 操作。
- 阅读区只渲染当前章节或必要文本块，不一次性插入整本小说。
- 自动滚动采用 `requestAnimationFrame + 实际时间差`。
- 进度限频保存，并在暂停、切书和关闭时强制保存。
- 阅读模式空闲时不得产生高频网络请求或日志。

## 12. 首版不包含

- EPUB、PDF、MOBI 等其他格式。
- 云端书库和多设备同步。
- 联网书源和小说下载。
- 文字朗读。
- 全局老板键、系统托盘和桌面悬浮窗。
- 伪造接口响应或虚假工作页面。
- 自定义加密系统。
- 全文搜索、书签和批注。

## 13. 后续增强

1. 分页阅读模式和自适应每页字数。
2. 章节目录、章节搜索和全文搜索。
3. 书签、阅读统计和进度重置。
4. 书库备份与恢复。
5. EPUB 支持。
6. 隐藏后继续滚动选项。
7. 字体、字重、字间距和主题预设。

## 14. 验收标准

- 可以导入 UTF-8 和 GB18030 TXT。
- 可以选择、切换和删除已导入书籍。
- 每本书分别保存进度，重启 WPS 后可以恢复。
- 自动滚动速度稳定，可随时播放、暂停和调整。
- 鼠标移出隐藏正文，返回后恢复显示。
- 阅读与排版切换不会丢失识别结果。
- 阅读期间 WPS 文档输入、Ribbon 和窗口切换正常。
- 小说只保存在用户本地目录，不上传网络。
- 日志、错误信息和请求中不包含小说正文或文件路径。
- 删除操作不会修改或删除用户原始 TXT。
- 现有预览排版、一键排版、清除预览和本机检测全部保持正常。

## 15. 发布阶段

1. 存储目录、SQLite、导入和书库管理。
2. 阅读界面、进度保存和模式切换。
3. 自动滚动、隐身模式和样式设置。
4. 自动测试与真实 WPS 验收。
5. 满足全部验收项后作为 5.4 候选版本发布。
