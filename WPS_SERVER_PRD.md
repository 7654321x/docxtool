# DocxTool WPS 公网服务第一阶段 PRD

## 1. 文档信息

- 产品名称：DocxTool WPS 公网服务
- 当前阶段：第一阶段自动实现完成，等待真实 WPS 验收
- 文档状态：已实现
- 实现状态：九轮代码、客户端打包和自动门禁已完成
- 版本归属：DocxTool 5.4
- 适用范围：WPS 插件用户注册登录、设备识别、在线状态、免费一键排版授权和次数统计
- 上位规则：仓库根目录 `AGENTS.md`、现有 Web 部署规范和 WPS 本机通信规范

## 2. 产品背景

当前 DocxTool WPS 插件的识别和排版均在用户电脑本地执行：

```text
WPS 侧边栏
→ 本机 EXE / Control Server
→ 本地 Recognition 与 DocxTool Engine
→ 本地保存、替换和重开文档
```

当前公网 Web 服务已经具有普通用户注册登录、管理员会话、任务记录和 SQLite 数据库，但这些账号主要服务于网页上传、模板和下载，不包含 WPS 设备、在线状态或插件排版请求。

本阶段需要为 WPS 插件建立独立的公网账号体系。用户注册登录后可以免费使用一键排版，但每次一键排版必须先经过公网服务器授权。服务器记录“哪个账号、哪台设备、何时请求了哪个功能”，并在后台形成排版次数统计。公文内容始终留在本机。

### 2.1 当前登录与性能规则

- WPS 启动器使用 `PySide2 5.15.2.1 + Qt Widgets + QSS`；每次启动都先显示登录注册窗口，自动登录只能在窗口显示后触发与用户点击“登录”相同的提交函数。
- 新注册和新登录签发的 WPS 会话由 24 小时调整为固定 7 天；心跳仍不续期，已有会话不迁移。
- 单进程 WPS 服务将注册哈希、真实或虚假账号校验及旧哈希升级统一限制为最多两个并发 Argon2 操作，密码安全参数不变。
- 旧哈希升级在 SQLite 写锁外完成计算，事务内只复核账号状态并写入预计算摘要。
- WPS 登录同一出口 IP 限制调整为每 10 分钟 300 次；单账号、注册和网页版限流保持原值。
- 本轮不增加异常识别、自动封禁、密码缓存、重试或新的公网协议字段。

## 3. 已确认产品决策

| 决策项 | 第一阶段结论 |
| --- | --- |
| 识别位置 | 用户本机 |
| 排版位置 | 用户本机 |
| 公文上传 | 不上传 |
| 用户体系 | WPS 插件独立注册登录 |
| 登录入口 | 用户端启动器的独立登录注册窗口，不放入 WPS TaskPane |
| 本地账号 | 默认显示登录窗口；可记住 DPAPI 密码并由用户选择自动登录 |
| 密码保存 | 服务器只存密码摘要；本机 SQLite 只存 Windows DPAPI 加密后的密码密文 |
| 账号规则 | 5 至 32 位，只允许英文字母和数字，且必须同时包含字母和数字 |
| 密码规则 | 5 至 64 位，只允许英文字母和数字，且必须同时包含字母和数字 |
| 账号唯一性 | 忽略英文字母大小写，全体 WPS 用户中唯一 |
| 当前价格 | 免费开放 |
| 本地功能 | 打开侧边栏、本机检测、界面设置、识别预览、小说阅读、清除预览 |
| 受控功能 | 一键排版及后续高级功能 |
| 会话有效期 | 新签发会话自成功登录起精确 7 天；已有会话按原到期时间生效 |
| 在线检测 | EXE 运行期间每 10 分钟发送轻量心跳 |
| 一键排版 | 每次执行前必须经过公网服务器授权 |
| 计数依据 | WPS 排版请求记录，不在用户表保存重复计数 |
| 数据库 | 新建 WPS 插件专用数据库 |
| 公网服务 | 与现有 Web 服务共用一个进程和公网入口 |
| 对外端口 | 继续统一使用 HTTPS 443，不新增第二个公网端口 |
| 管理后台 | 重建统一管理员工作台，分别管理网页业务库和 WPS 插件库 |
| 卡密 | 第一阶段不实现，后续用于兑换有效期和高级功能 |

## 4. 产品目标

1. 用户可以在独立启动窗口中注册和登录，并在插件中查看当前账号状态。
2. 登录或注册成功后生成一份新的、精确 7 天有效的 WPS 会话；心跳不续期。
3. 服务器能够识别账号使用的设备、插件版本和最后在线时间。
4. EXE 运行期间发送轻量心跳，后台可以区分在线和离线设备。
5. 当前所有正常账号均免费获得一键排版权限。
6. 每次一键排版必须先经过服务器授权，服务器创建唯一请求记录后才返回允许执行。
7. 服务器向本地 Engine 下发当前格式配置及配置版本，不下发可执行代码。
8. 本地排版完成后回传成功或失败状态，后台能够统计总次数、成功次数和失败次数。
9. WPS 用户、设备、会话和排版请求与网页端业务数据库隔离。
10. 不改变 Recognition、Normalizer、DocxTool Engine、WPS 文档事务和本机 HostBridge 的职责。
11. 重建管理员页面，使网页业务与 WPS 插件业务在同一工作台中清晰分区，不能继续混在单一监控页面中。
12. 用户端每次启动都显示独立登录注册窗口；记住密码用于回填，自动登录只在窗口显示后提交一次，不得跳过窗口。

## 5. 第一阶段非目标

- 不实现卡密生成、卡密批次、卡密兑换和付费套餐。
- 不实现支付、订单、退款、发票或代理商系统。
- 不实现离线一键排版；公网服务器不可用时，受控功能不可执行。
- 不上传 DOCX、正文、文件路径、图片、表格或识别结果；仅允许在一键排版的结果回传中上传已校验的基础文件名，供管理员后台查看任务。
- 不把 Recognition 或 DocxTool Engine 移到公网服务器。
- 不允许公网服务器下发 Python、JavaScript、Shell、OOXML 补丁或任意脚本。
- 不统一网页账号和 WPS 插件账号。
- 不新增第二套本地 WPS 通信协议。
- 不新增第二个公网域名或公网端口。
- 不在第一阶段实现配额扣减、并发席位或按次数收费。

## 6. 总体架构

### 6.1 公网服务

第一阶段采用一个公网入口、一个 Python 服务进程、两个独立数据库：

```text
WPS / 浏览器
└─ https://docx.toolpp.cn
   └─ Cloudflare Pages Worker
      └─ https://origin.toolpp.cn (DNS A: 43.130.232.115)
         └─ Nginx :443
            └─ 127.0.0.1:9527 DocxTool Python 后端
               ├─ 现有网页接口（var/data/stats.db）
               └─ /wps-api/v1/*（var/data/wps_plugin.db）
```

WPS 插件接口使用稳定路径前缀 `/wps-api/v1`，正式客户端只连接 Pages HTTPS Origin。后续需要独立扩容时，可以调整 Worker 后的内部服务路由，不改变客户端地址，也不新增直连公网入口。

### 6.2 用户端

```text
WPS TaskPane
→ 本机 DocxToolWps.exe
→ HTTPS /wps-api/v1/*
→ 公网服务器授权并返回格式配置
→ 本机 HostBridge
→ 本地 Recognition / DocxTool Engine
```

用户端不分发 wheel、Python 源码、测试、数据库或永久服务器密钥。正式客户端由一个 EXE 启动本地插件服务；WPS 本机静态端口和随机 Control 端口继续只监听回环地址。

用户端可以保存一份只属于当前 Windows 用户的本地账号数据库。该数据库不是公网业务数据库，不随服务器部署，不上传服务器，也不与 `stats.db` 或 `wps_plugin.db` 混用。

## 7. 功能边界

### 7.1 本地直接功能

以下功能不需要公网服务器授权：

- 打开侧边栏。
- 本机检测。
- 界面设置。
- 识别预览。
- 小说阅读。
- 清除预览。

这些功能不得因公网服务器不可用而失效。

### 7.2 服务器受控功能

第一阶段只有“一键排版”属于受控功能。后续高级功能继续复用同一授权入口，通过功能名称区分，不新增任意命令接口。

受控功能必须满足：

1. WPS 用户会话有效。
2. 用户状态正常。
3. 设备状态正常。
4. 请求功能在服务器允许列表中。
5. 服务器已经创建唯一请求记录。
6. 服务器返回允许执行和格式配置后，本地 Engine 才能运行。
7. 服务器授权必须早于旧格式静默升级和任何文档内容改写。

## 8. 核心用户流程

### 8.1 注册

1. 用户启动 `DocxToolWps.exe`，启动器读取本地账号；首次使用时没有已保存账号。
2. 启动器弹出独立登录注册窗口，用户选择注册；窗口不嵌入 WPS TaskPane。
3. 用户输入登录账号、密码和确认密码。
4. 启动窗口先提示账号和密码格式；密码确认不一致时不提交。
5. 本机 EXE 将注册请求发送到公网服务器。
6. 服务器独立校验账号和密码格式，不能依赖客户端校验结果。
7. 服务器将账号转为小写标准化值，在事务中检查是否已存在，并由数据库唯一约束再次保证不会重复。
8. 账号不存在时创建 WPS 用户；已存在时返回“账号已存在”。
9. 注册成功后自动创建 7 天会话并绑定当前设备。
10. EXE 使用 Windows DPAPI 加密密码、会话凭据和设备密钥后写入本地 SQLite。
11. 登录注册窗口关闭，EXE 启动本地 Control Server 和 WPS 插件服务。

服务器数据库继续只保存 Argon2id 密码摘要，不保存或返回用户密码。

### 8.2 登录

1. 启动器每次启动都先显示独立登录注册窗口；自动登录仅在用户明确启用且窗口已经显示后生效。
2. 本机已有账号时预填账号名并复用原设备密钥；记住密码开启时回填 DPAPI 解密密码，否则密码为空。
3. 启动窗口先做同一格式提示，服务器收到请求后再次权威校验。
4. 服务器按账号小写标准化值查询用户，并使用 Argon2id 校验提交密码与数据库密码摘要。
5. 账号不存在、账号格式错误和密码错误统一返回“账号或密码错误”，不暴露账号是否存在。
6. 密码正确后继续验证用户状态和设备状态。
7. 登录成功后创建新的 7 天会话。
8. 原始会话凭据只返回一次，服务器仅保存摘要。
9. EXE 使用 Windows DPAPI 加密密码、会话凭据和设备密钥后写入本地 SQLite。
10. 登录成功后关闭窗口并启动插件；用户主动关闭窗口时停止本次启动。

### 8.3 启动、本地账号与心跳

1. EXE 启动后读取本地账号数据库并始终显示登录注册窗口；账号库损坏时先隔离损坏文件。
2. `auto_login=false` 时等待用户手动登录或注册；用户关闭窗口时不启动插件。
3. `auto_login=true` 时在窗口显示后使用已记住密码触发一次与登录按钮相同的提交；失败后保持窗口并允许手动修改，不循环提交。
4. 公网认证未成功时不创建 AccountRuntime、Control Server、静态服务或 WPS 加载项注册。
5. 登录或注册成功后使用新会话启动 AccountRuntime，并立即发送一次心跳。
6. 之后仅在 EXE 运行期间每 10 分钟发送一次心跳。
7. 运行期间会话到期时，AccountRuntime 可以使用 DPAPI 解密的已保存密码刷新会话；该行为不用于下一次 EXE 启动时跳过登录窗口。
8. 账号或设备被服务器明确拒绝时，删除本地账号及其待发排版结果。
9. 心跳更新会话最后活动时间、设备最后在线时间、插件版本和最近访问地址。
10. 后台将最后在线时间在 30 分钟以内的设备显示为“在线”，否则显示为“离线”。
11. 单次网络心跳失败不删除本地账号；客户端只记录网络状态变化，不高频重复输出同一错误。
12. 心跳不会延长 7 天会话；运行期刷新成功后签发一份新的 7 天会话。服务升级前已有会话不迁移，按数据库原到期时间自然失效。

### 8.4 本地账号保存

- 本机只保存一个当前账号，位置固定在用户本地应用数据目录下。
- 登录账号、设备编号和会话到期时间可以明文保存。
- 密码、原始会话凭据和设备密钥必须先通过 Windows DPAPI 按当前 Windows 用户加密，再作为 BLOB 写入 SQLite。
- 不得把服务器数据库中的 Argon2id 密码摘要下载到本机，也不得把密码摘要当成登录凭据。
- 本地 SQLite 文件被复制到另一台电脑或另一个 Windows 用户后，密文不得解密。
- 用户退出或切换账号时删除当前本地账号记录和对应密文。

### 8.5 一键排版授权

1. 用户点击一键排版。
2. EXE 只采集账号、设备、功能和客户端版本，不读取或上传文档内容。
3. EXE 生成本次唯一请求编号，向公网服务器提交授权请求。
4. 服务器验证会话、用户、设备、功能和客户端版本。
5. 服务器按请求编号创建一条“已授权”排版请求记录。
6. 相同请求编号重试时返回原记录，不重复增加次数。
7. 服务器返回允许执行、请求编号、格式配置版本和格式配置。
8. 授权成功后，本机才执行现有文档格式检测、保存和旧格式升级前置流程。
9. EXE 将格式配置传给现有本地排版入口。
10. 本地 Engine 按现有事务完成排版、替换和重开。
11. EXE 使用同一请求编号回传成功或失败结果。
12. 服务器更新请求状态、完成时间、耗时和脱敏错误代码。

### 8.6 会话或权限无效

- 会话过期：显示“登录已过期，请重新登录”。
- 用户停用：显示“账号已停用”。
- 设备停用：显示“当前设备已停用”。
- 服务器不可用：显示“暂时无法验证一键排版权限”。
- 格式配置无效：拒绝启动 Engine，并显示独立错误。
- 已经进入本地文档替换事务后，不因会话变化中断；当前事务必须完成或回滚。

## 9. 数据库方案

### 9.1 数据库隔离

新增独立数据库：

```text
var/data/wps_plugin.db
```

通过独立环境变量配置：

```text
WPS_DATABASE_PATH=var/data/wps_plugin.db
```

该数据库使用独立连接工厂和锁，不与现有 `stats.db` 开启跨库事务。SQLite 继续使用 WAL 模式。

### 9.2 WPS 用户表

职责：保存插件用户的基础账号信息。

- 用户编号。
- 登录账号。
- 账号标准化值。
- 密码摘要。
- 账号状态。
- 注册时间。
- 更新时间。
- 最后登录时间。

约束：

- 登录账号长度为 5 至 32 位，只允许 ASCII 英文字母和数字，并且至少包含一个字母和一个数字。
- 账号标准化值只将 ASCII 大写字母转为小写；`User01` 和 `user01` 视为同一账号。
- 注册时先在事务内查询标准化账号，再依赖 `username_norm UNIQUE` 处理并发重复注册。
- 密码长度为 5 至 64 位，只允许 ASCII 英文字母和数字，并且至少包含一个字母和一个数字。
- 密码只保存 Argon2id 摘要；账号状态至少支持正常和停用。
- 以上规则只适用于 WPS 插件账号，不修改现有网页账号规则。

### 9.3 WPS 设备表

职责：保存账号绑定设备和在线状态。

- 设备编号。
- 所属用户。
- 设备指纹摘要。
- 设备名称。
- 操作系统。
- 插件版本。
- 设备状态。
- 首次绑定时间。
- 最后在线时间。
- 最近访问地址。

约束：同一用户下的设备指纹摘要唯一；不保存原始硬件序列号；设备状态至少支持正常和停用。

### 9.4 WPS 会话表

职责：保存用户登录后的固定期限身份状态。

- 会话摘要。
- 所属用户。
- 使用设备。
- 登录时间。
- 最后活动时间。
- 到期时间。
- 登录地址。
- 客户端版本。

约束：服务器只保存会话摘要；新会话到期时间固定为签发时间后 7 天；心跳不自动延长；已有会话不批量修改 `expires_at`。

### 9.5 WPS 排版请求表

职责：记录每一次被服务器接受的一键排版请求，并作为统计的唯一事实来源。

- 请求编号。
- 请求用户。
- 请求设备。
- 功能名称。
- 执行状态。
- 配置版本。
- 请求时间。
- 完成时间。
- 执行耗时。
- 错误代码。
- 客户端版本。
- 基础文件名（可选，仅由实际执行排版的 WPS Host 返回；不包含路径）。

约束：请求编号全库唯一；只保存受限基础文件名，不保存文档路径、正文、哈希或识别结果；执行状态至少支持已授权、成功和失败。历史记录或旧客户端没有名称时保持为空，后台显示为 `-`。

### 9.6 统计口径

不在 WPS 用户表保存“排版次数”字段。后台按排版请求表实时统计：

- 排版总次数：该用户所有已授权请求数量。
- 成功次数：执行状态为成功的请求数量。
- 失败次数：执行状态为失败的请求数量。
- 待回报次数：已授权但尚未收到最终结果的请求数量。
- 最后排版时间：该用户最近一次已授权请求时间。

会话无效、用户停用或设备停用导致的拒绝请求不计入用户排版次数，只进入脱敏服务日志。

## 10. 公网接口

### 10.1 用户接口

```http
POST /wps-api/v1/auth/register
POST /wps-api/v1/auth/login
GET  /wps-api/v1/auth/me
POST /wps-api/v1/auth/logout
```

注册和登录使用账号密码。登录成功后返回用户摘要、会话凭据和精确到期时间。后续请求使用 Bearer 会话凭据，不复用网页 Cookie 和 CSRF 会话。

注册字段检测：

- `username`：5 至 32 位，只允许 `A-Z`、`a-z`、`0-9`，且必须同时包含字母和数字。
- `password`：5 至 64 位，只允许 `A-Z`、`a-z`、`0-9`，且必须同时包含字母和数字。
- 未知字段、空字符串、首尾空格和不符合类型的值直接拒绝，不自动修复。

注册账号按小写标准化值查重。登录时无论账号不存在、账号格式错误或密码错误，对外均返回统一错误，服务器日志只记录失败阶段和脱敏账号摘要，不记录账号密码。

### 10.2 设备与在线接口

```http
POST /wps-api/v1/heartbeat
```

心跳只提交用户会话、设备编号、客户端版本和必要运行状态。服务器返回账号状态、设备状态、服务器时间和当前配置版本。

### 10.3 排版授权接口

```http
POST /wps-api/v1/format/authorize
POST /wps-api/v1/format/result
```

授权成功响应包含：

- 是否允许执行。
- 请求编号。
- 功能名称。
- 格式配置版本。
- 格式配置。
- 服务器时间。

结果接口回传请求编号、成功或失败、执行耗时、客户端版本、脱敏错误代码，以及可选的基础文件名。该名称由实际执行命令的 WPS Host 获取，不能包含路径；网络不可用时与结果一起保存在本机 outbox 后补报。

### 10.4 接口共同要求

- 所有响应均为版本化 JSON 对象。
- 所有请求使用 HTTPS。
- 请求编号贯穿公网服务器、本机 EXE、HostBridge 和本地日志。
- 错误必须区分注册、登录、会话、设备、心跳、授权、配置和结果回传阶段。
- 未知字段、未知命令和无效状态立即拒绝，不做猜测性兼容。
- 服务端不得把管理员密钥、数据库路径或内部异常堆栈返回客户端。

## 11. 格式配置下发

1. 第一阶段服务器只维护一份当前正式格式配置。
2. 格式配置沿用现有 `format_config` 结构，不新增第二套排版配置协议。
3. 服务器为每个配置分配稳定配置版本。
4. 一键排版授权成功时返回完整配置及配置版本。
5. 本地 EXE 按现有 JSON 校验规则验证配置，通过后才调用 Engine。
6. 第一阶段服务器配置必须与当前稳定默认配置保持一致，避免识别预览和正式排版产生无解释差异。
7. 服务器只能下发数据配置，不能下发脚本、路径或任意 OOXML 修改指令。

## 12. 管理后台

第一阶段重建现有管理员页面。新后台使用统一登录、统一导航和统一视觉外壳，但按数据库拆成两个独立业务模块，不能只在旧监控页中增加一块 WPS 表格。

```text
管理员登录
└─ DocxTool 管理工作台
   ├─ 综合概览
   ├─ 网页业务
   │  └─ stats.db
   └─ WPS 插件
      └─ wps_plugin.db
```

### 12.1 综合概览

综合概览只显示两个模块的入口、数据库运行状态和各自核心摘要：

- 网页业务：任务总数、成功数、失败数和当前排队数。
- WPS 插件：用户总数、在线设备数、排版请求总数和待回报数。

概览可以在应用层组合两个独立查询结果，但不得使用 SQLite `ATTACH`、跨库 JOIN 或跨库事务。

### 12.2 网页业务模块

保留现有任务、运行状态、活跃 IP、封禁、上传限额和日志查询能力，数据仍只来自 `stats.db`。现有 `/monitor` 地址保留为兼容入口，但登录后默认进入新的管理工作台。

### 12.3 WPS 用户列表

向管理员展示：

- 登录账号。
- 账号状态。
- 绑定设备数。
- 当前在线状态。
- 注册时间。
- 最后登录时间。
- 排版总次数。
- 成功次数。
- 失败次数。
- 待回报次数。
- 最后排版时间。
- 当前插件版本。

支持按账号和状态搜索，支持启用或停用账号。

### 12.4 WPS 用户详情

展示该用户的设备列表和排版请求列表。设备列表支持停用和恢复设备；排版请求列表支持按时间和状态查看，不提供文档内容入口。

### 12.5 数据访问边界

- 管理员登录和 CSRF 继续复用现有管理员会话。
- 网页业务页面只能使用 `stats.db` 连接工厂和锁。
- WPS 插件页面只能使用 `wps_plugin.db` 连接工厂和锁。
- 页面外壳可以共用，查询函数、统计口径和状态修改函数不能共用数据库连接。
- 任一页面必须明确标注当前管理的是“网页业务”还是“WPS 插件”。

### 12.6 第一阶段不提供

- 卡密生成按钮。
- 卡密批次管理。
- 权益续期。
- 收费套餐。
- 次数扣减。
- 远程触发用户电脑排版。

## 13. 日志与隐私

### 13.1 允许记录

- 用户短编号。
- 设备短编号。
- 会话短编号。
- 请求编号。
- 功能名称。
- 配置版本。
- 客户端版本。
- 状态、耗时和脱敏错误代码。
- 已校验的基础文件名，仅保存于排版请求记录并在管理员后台展示。

### 13.2 禁止记录或上传

- 用户密码和原始会话凭据。
- 原始设备硬件序列号。
- DOCX、正文、识别结果和批注内容。
- 文档绝对路径和本地目录。
- 公网服务器私钥、管理员密钥和数据库凭据。
- 本地 Engine 内部完整异常堆栈。

### 13.3 必要日志事件

- 注册开始、成功和失败。
- 登录开始、成功和失败。
- 会话创建、过期和退出。
- 设备首次绑定、心跳、停用和恢复。
- 排版授权开始、允许和拒绝。
- 格式配置返回和校验失败。
- 排版结果成功、失败和结果回传失败。

基础文件名不进入服务端或 WPS 本地日志、错误消息、审计日志或通知正文。

正常心跳不逐次写 INFO；只更新数据库。设备上线、离线、停用或错误状态变化时记录一次事件。

## 14. 失败边界

- 注册失败不得创建用户或会话。
- 登录失败不得暴露账号是否存在。
- 客户端格式校验通过不代表注册或登录成功，服务器必须重复执行全部账号密码校验。
- 重复账号检测必须同时依靠事务查询和数据库唯一约束，不能只做一次先查后写。
- 密码校验必须使用 Argon2id `verify_password()`，不能比较明文、可逆密文或客户端计算的摘要。
- WPS 注册哈希、真实或虚假账号校验及哈希升级必须共用单进程两个 Argon2 槽位；等待请求自然排队。Argon2id 参数保持 `m=65536 KiB、t=3、p=4`，网页版认证不使用该限制。
- 登录需要升级旧密码哈希时，必须先在 SQLite 写锁外计算新摘要，再进入短事务复核账号状态并写入；不得在 `BEGIN IMMEDIATE` 持有期间执行 Argon2。
- 会话创建失败不得返回假登录成功。
- 心跳失败不得中止 EXE 或本地功能。
- 排版授权失败不得触发旧格式升级、文档改写或本地 Engine。
- 请求记录创建失败不得返回允许执行。
- 格式配置读取或校验失败不得启动本地 Engine。
- 本地排版失败必须回滚现有文档事务并回传失败状态。
- 结果回传失败不得把本地排版改判为失败；客户端保留明确的“结果未同步”状态。
- 用户或设备在排版事务运行中被停用时，不中断当前事务，只影响下一次授权。

## 15. 部署方案

### 15.1 第一阶段

- 保留现有 Python 后端内部端口 `9527`。
- Cloudflare Pages 是唯一公网入口；Worker 注入 `X-Proxy-Secret` 后经 `https://origin.toolpp.cn`、Nginx 回源到 loopback 后端，不启用 Cloudflare Tunnel 或 Access。
- 新增 `/wps-api/v1/*` 路由，不开放新的公网端口。
- 网页业务继续使用 `stats.db`。
- WPS 业务使用 `wps_plugin.db`。
- 两个数据库分别备份、恢复和执行完整性检查。

### 15.2 后续拆分条件

出现以下情况之一时，再考虑把 WPS API 拆到独立内部端口：

- WPS 心跳量显著影响网页请求。
- WPS 服务需要独立发布节奏或可用性目标。
- SQLite 写入量超过单机合理范围，需要迁移到服务型数据库。
- WPS 管理后台需要独立部署。

拆分后仍保持公网路径 `/wps-api/v1/*`，由 Worker 后的内部路由改变上游，用户端无需升级接口地址。

## 16. 实施顺序

### 里程碑一：先完成 WPS 公网服务器

1. 新增独立数据库路径、连接和四张核心表。
2. 完成 WPS 注册、登录、当前用户和退出接口。
3. 完成设备绑定、7 天会话和心跳接口。
4. 完成免费一键排版授权接口。
5. 完成格式配置和配置版本下发。
6. 完成排版结果回传和统计查询。
7. 重建统一管理员工作台，并迁移现有网页业务监控入口。
8. 完成 WPS 用户列表、详情、启停账号和启停设备。
9. 使用模拟客户端完成服务端全流程测试。

该里程碑不得修改 WPS Host Runtime、Recognition、Normalizer 或 DocxTool Engine。

### 里程碑二：接入用户端 EXE

1. 增加启动器独立登录注册窗口，不修改 TaskPane 增加登录表单。
2. 增加本地账号 SQLite 和 Windows DPAPI 密文保存。
3. 启动时默认显示窗口，并增加 DPAPI 记住密码和用户可选自动登录。
4. 增加设备注册、运行期会话刷新和心跳。
5. 在一键排版前调用公网授权接口。
6. 将服务器格式配置传给现有本地排版入口。
7. 排版完成后回传结果。
8. 验证关闭窗口或公网登录失败时不启动插件。

### 里程碑三：真实 WPS 验收

1. 注册、登录、重启 EXE 和会话到期。
2. 后台在线状态和设备版本更新。
3. 免费一键排版授权、成功和失败统计。
4. 停用账号和设备后的下一次请求拒绝。
5. 文档未上传、日志不包含文档内容。
6. 现有识别预览、清除预览、阅读模式和本地检测不受影响。

## 17. 第一阶段验收标准

- WPS 用户可以独立注册和登录，不占用网页端用户表。
- 账号和密码均至少 5 位、同时包含字母和数字，其他字符被服务器拒绝。
- `User01` 与 `user01` 不能注册为两个账号，并发注册也只能成功一个。
- 登录时服务器能够正确区分有效密码与错误密码，但对外不暴露账号是否存在。
- 新登录会话自签发起精确 7 天有效，升级前已有会话仍按原到期时间失效。
- 默认显示独立登录注册窗口，WPS TaskPane 中不存在登录注册表单。
- 本机已有账号时窗口预填账号名并复用同一设备密钥；记住密码和自动登录按用户偏好生效。
- 用户关闭窗口或公网登录未成功时不启动插件。
- 本地 SQLite 中不存在明文密码、明文会话凭据或明文设备密钥。
- 同一设备重复登录不会重复创建设备记录。
- 后台能够显示设备在线状态和插件版本。
- 正常账号可以免费申请一键排版授权。
- 相同请求编号重复提交不会重复计数。
- 每次已授权请求都能在后台查到对应用户和设备。
- 成功、失败和待回报数量计算准确。
- 停用账号或设备后，下一次授权请求立即拒绝。
- 授权被拒绝时，当前文档及其文件格式不发生变化。
- 服务器返回当前格式配置及稳定配置版本。
- 公网请求和数据库中不存在文档内容、名称或路径。
- `stats.db` 与现有网页注册、登录、上传、模板和任务功能不受影响。
- 管理员登录后进入统一工作台，网页业务和 WPS 插件业务有明确的一级导航。
- 网页业务页面只读取 `stats.db`，WPS 页面只读取 `wps_plugin.db`，不存在跨库 JOIN 或跨库事务。
- 原 `/monitor` 地址继续可用，不破坏现有管理员书签。
- 不新增第二个公网端口。
- 自动测试覆盖注册、登录、会话过期、设备、心跳、授权、重复请求、结果回传、统计和停用场景。

## 18. 后续卡密与权益阶段

第一阶段稳定后再增加：

```text
卡密批次
→ 卡密生成
→ 用户兑换
→ 账号权益与到期时间
→ 功能权限
→ 设备数量限制
```

后续数据库至少需要增加卡密批次、卡密和用户权益三类数据。卡密只用于兑换，不作为日常登录凭据；兑换后的一键排版仍沿用本 PRD 的设备、会话、心跳、授权和请求记录流程。

## 19. 验证入口

服务端实现后至少验证：

1. WPS 数据库结构、唯一约束、索引和 SQLite 完整性。
2. WPS 注册登录接口及密码摘要。
3. 7 天新会话、历史到期时间兼容与账号、设备停用。
4. 心跳更新与在线状态计算。
5. 排版授权、重复请求、结果回传和统计。
6. 管理后台账号、设备和请求查询。
7. 现有 Web 账号、上传、任务、模板和管理员门禁。
8. 生产 Pages、Access、Tunnel 路由、HTTPS、数据库路径和备份恢复。

用户端 EXE 接入与自动测试完成后，可以声明 WPS 公网服务与客户端自动验收通过；真实 WPS 操作未完成前，不能声明完整产品验收通过。
